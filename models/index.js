// Importing User and Thought models
const User = require('./User');
const Thought = require('./Thought');

// Exporting User and Thought models for use in other parts of the app
module.exports = { User, Thought };
